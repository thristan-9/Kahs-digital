import style from "../../styles/Faqs.module.css";

export default function Faqs() {
    return (
        <section id="faqs" className={style.faqs}>
            <div className={`${style.faqs__container} container grid`}>
                <div className={`${style.faqs__data}`}>
                   
                </div>
            </div>
        </section>
    );
}
